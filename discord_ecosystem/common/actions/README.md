# Actions

## What is an action ?

An action is an **Atomic** process, which will execute ONE action on discord, and does not depend on interaction.
It can be executed by a command and an API in the same way.

example: 
*Creating a channel on a server* is an action.
*Answering to a message* is not.
