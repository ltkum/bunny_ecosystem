services:
  discord_ecosystem:
    build:
      context: ./discord_ecosystem
      dockerfile: Dockerfile

    container_name: discord_ecosystem

    env_file:
      - .discord.env

    restart: unless-stopped
